#### To change vim configuration on linux:
* Place file ```.vimrc``` under ```~/``` (```/home/usr```).
* Place directory ```.vim``` under ```~/```.
* Restart vim.

<!--
- Edit system-wise vim configuration file:
    ```bash
    sudo vim /etc/vim/vimrc
    ```
-->